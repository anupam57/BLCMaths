jobs:
  initialize:
    uses: ./.github/workflows/ci_cd_initialize.yml
    with:
      project-name: PROJECT_NAME
      module: MODULE_NAME

  promote-nb:
    needs: [ initialize ]
    with:
      environment: nb

  promote-sa:
    needs: [ initialize, promote-nb ]
    with:
      environment: sa

